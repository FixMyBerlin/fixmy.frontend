import styled from 'styled-components';

const Card = styled.div`
  background: #fffaed;
  padding: 16px;
  box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.25);
  max-width: 650px;
  margin: 0 auto;
`;

export default Card;
