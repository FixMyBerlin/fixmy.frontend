import styled from 'styled-components';
import config from '~/config';

const Text = styled.p`
  font-weight: 400;
  font-family: ${config.baseFont};
  margin: 1em 0;
  line-height: 1.5;
`;

export default Text;
